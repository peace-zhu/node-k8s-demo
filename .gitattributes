# ============================================================================
# .gitattributes —— 锁定换行符，避免跨平台灾难
# ============================================================================
#
# 【问题的由来】
#   Windows 用 CRLF（\r\n）作为换行，Linux/macOS 用 LF（\n）。
#   如果不管，会出现两种翻车：
#
#   ① 在 Windows 上提交、在 Linux 上 clone → 文件带 CRLF
#      → Dockerfile 里可能有 `RUN` 指令因尾部 \r 而报奇怪的错
#      → shell 脚本报 `bad interpreter: /bin/sh^M: no such file or directory`
#      → 在 K8s 里挂进去当配置，程序可能把 \r 当成内容的一部分
#
#   ② git diff 显示"整个文件都改了"，其实只是换行符变了 → 代码评审失效
#
# 【本文件的解法】
#   `* text=auto eol=lf` = 所有文本文件在**仓库里和工作区里都用 LF**。
#   二进制文件按原样处理，不做任何转换。
#
#   这样 Windows 和 Linux 上看到的字节完全一致，跨平台零意外。
#   ⚠️ 如果将来要写 Windows 批处理脚本（.bat/.cmd），单独给它们指定 crlf。
# ============================================================================

# 默认：文本文件统一用 LF
* text=auto eol=lf

# Windows 批处理脚本必须用 CRLF（当前项目没有，先留着规则）
*.bat text eol=crlf
*.cmd text eol=crlf

# 以下按二进制处理，禁止任何换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
*.ttf binary
*.zip binary
*.gz binary
*.tar binary

# 文档在 GitHub 上按纯文本渲染
*.md text

# 让 GitHub 的语言统计更准确
Dockerfile linguist-language=Dockerfile
