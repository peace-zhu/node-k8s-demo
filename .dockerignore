# ============================================================================
# .dockerignore —— 决定「哪些文件不进镜像」
# ============================================================================
# 它只影响 `docker build` 时送进构建上下文的文件集合，不影响 git。
#
# 🔴 最重要的一条：**必须挡住 .env**
#    密钥一旦被打进镜像并推到仓库，任何人都能 pull 下来看到，而且删不掉
#    （镜像分层，历史层里还留着 —— 只能改密钥 + 重新构建）。
# ============================================================================

# 依赖目录：容器里会重新安装，不要从宿主机带进去
node_modules

# 敏感文件
.env
.env.*
*.pem
*.key
*.p12

# 版本控制与文档（运行镜像里不需要）
.git
.gitignore
*.md

# Kubernetes 清单：它们是"怎么部署"的说明书，不是应用的一部分
k8s/

# 构建相关的文件本身不需要进镜像
Dockerfile
.dockerignore

# 日志与临时文件
*.log
npm-debug.log*
.DS_Store
